{
  "routes": [
    {
      "route": "/home",
      "allowedRoles": ["authenticated"]
    }
  ],
  "navigationFallBack": {
    "rewrite": "index.html",
    "exclude": ["/assets/*", "/*.js", "/*.scss", "/*.ico"]
  }
}
