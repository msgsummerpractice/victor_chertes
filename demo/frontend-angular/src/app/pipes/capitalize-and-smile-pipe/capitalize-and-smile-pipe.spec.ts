import { CapitalizeSmilePipePipe } from './capitalize-and-smile-pipe';

describe('CapitalizeSmilePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeSmilePipePipe();
    expect(pipe).toBeTruthy();
  });
});
