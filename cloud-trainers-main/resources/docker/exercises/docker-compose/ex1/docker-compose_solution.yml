version: '3.8'

services:
  redis-server:
    image: 'redis:alpine'
    volumes:
      - redis-data:/data

  node-app:
    build: .
    ports:
      - '8081:8081'
    environment:
      - REDIS_HOST=redis-server
    depends_on:
      - redis-server

volumes:
  redis-data:
